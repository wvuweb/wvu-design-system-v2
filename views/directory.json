{%- assign people = page.children -%}
{%- assign i = 0 -%}
{
    {% for person in people %}
    "person" : {
        "name": "{{ person.name }}"
    }{% if i < people.size %},{% endif %}
    {%- assign i = i | plus: 1 -%}
    {% endfor %}
}