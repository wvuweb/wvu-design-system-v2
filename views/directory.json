{%- assign people = page.children -%}
{%- assign i = 0 -%}
{
    {% for person in people %}
    {%- assign i = i | plus: 1 -%}
    "person" : {
        "name": "{{ person.name }}"
    }{% if i < people.size %},{% endif %}
    {% endfor %}
}