<?xml version="1.0" encoding="UTF-8"?>

<profiles>
  {% for profile in page.children %}
    <profile>
      <name>{{ profile.alternate_name | default: profile.name }}</name>
      <image_url>{{ profile.data.thumbnail_url }}</image_url>
      <url>{{ profile.url }}</url>
      <job_title>{{ profile.content['wvu-backpage-header-1__subhead'] }}</job_title>
      <phone>{{ profile.content['wvu-profile-1__phone'] }}</phone>
      <email>{{ profile.content['wvu-profile-1__email'] }}</email>
      <short_description>{{ profile.data.description }}</short_description>
      <quote>{{ profile.content['wvu-profile-1__quote'] }}></quote>
    </profile>
  {% endfor %}
</profiles>
